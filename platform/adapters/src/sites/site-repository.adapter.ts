/**
 * SiteRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: sites
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * SiteRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements SiteRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  SiteRepository,
} from "@tierfog/services/sites";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { SiteRepositoryDdb } from "./site-repository.ddb.js";

/**
 * SiteRepositoryAdapter
 *
 * Implements SiteRepository port for infrastructure layer.
 */
export class SiteRepositoryAdapter implements SiteRepository {
  private readonly ddb: SiteRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new SiteRepositoryDdb(this.dynamoClient);
  }

  async listSites(input: Parameters<SiteRepository['listSites']>[0]): Promise<Awaited<ReturnType<SiteRepository['listSites']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listSites(input);
  }

  async registerSite(input: Parameters<SiteRepository['registerSite']>[0]): Promise<Awaited<ReturnType<SiteRepository['registerSite']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.registerSite(input);
  }

  async getSite(input: Parameters<SiteRepository['getSite']>[0]): Promise<Awaited<ReturnType<SiteRepository['getSite']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getSite(input);
  }

  async updateSite(input: Parameters<SiteRepository['updateSite']>[0]): Promise<Awaited<ReturnType<SiteRepository['updateSite']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.updateSite(input);
  }
}
