/**
 * ExecuteGetTier - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: tiers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetTier Use Case
 *
 * DDD: Application use case for get Tier.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetTierInput, GetTierOutput } from "../dto/tier.dto";
import type { ExecutionContextService, IdGeneratorService } from "@tierfog/services/_shared/index.js";
import type { TierRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetTier {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly tier: TierRepository
  ) {}

  async execute(input: GetTierInput): Promise<GetTierOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.tirId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).tierId) {
      throw new ValidationError("tierId is required");
    }
    const tierId = (input as any).tierId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.tier.getTier({ tierId: tierId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${tierId}`);
    }

    return entity as GetTierOutput;
  }
}
