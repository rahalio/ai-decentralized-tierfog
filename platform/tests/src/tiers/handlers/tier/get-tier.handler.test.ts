/**
 * GetTier handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: tiers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getTier } from "@tierfog/api-server/domains/tiers/handlers";
import type { TiersDDDDependencies } from "@tierfog/api-server/domains/tiers";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@tierfog/tests/shared";
import { TIERS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetTier", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TiersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TiersDDDDependencies>(
      TIERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetTier, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { tierId: entity.id };

    await getTier(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetTier.execute).toHaveBeenCalledWith(expect.objectContaining({ tierId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { tierId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetTier, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getTier(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
