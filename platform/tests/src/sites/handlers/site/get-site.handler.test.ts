/**
 * GetSite handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: sites
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSite } from "@tierfog/api-server/domains/sites/handlers";
import type { SitesDDDDependencies } from "@tierfog/api-server/domains/sites";
import { createSite as createSiteEntity } from "@tierfog/factories/sites";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@tierfog/tests/shared";
import { SITES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSite", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SitesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SitesDDDDependencies>(
      SITES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createSiteEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSite, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { siteId: entity.id };

    await getSite(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSite.execute).toHaveBeenCalledWith(expect.objectContaining({ siteId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { siteId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetSite, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getSite(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
